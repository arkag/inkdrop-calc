// pulled from inkdrop-math plugin

'use babel'
import { markdownRenderer } from 'inkdrop'

module.exports = {
  activate() {
    if (markdownRenderer) {
      // SOMETHING
    }
  },

  deactivate() {
    if (markdownRenderer) {
      // SOMETHING
    }
  }
}
